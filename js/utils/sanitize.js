/* OPJ Elite — échappement HTML pour tout contenu injecté en innerHTML */
'use strict';
function eh(s) {
  if (s == null) return '';
  return String(s)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}
